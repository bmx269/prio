<div class="overlay-container">
  <div class="bread-nav">
    <div class="container-fluid">
      <div class='row'>
        {{#link-to 'connections.in-the-news' class='btn btn-outline-primary'}}In The News{{/link-to}}
      </div>
    </div>
  </div>
  {{#each page.section as |section|}}
    {{page-section section=section}}
  {{/each}}
  {{article-archive articles=articles}}
  {{liquid-outlet enableGrowth=false}}
</div>
