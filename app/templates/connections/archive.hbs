<div class="overlay-container">
  <div class="bread-nav">
    <div class="container-fluid">
      <div class='row'>
        <LinkTo @route="connections.in-the-news" class="btn btn-outline-primary">In The News</LinkTo>
      </div>
    </div>
  </div>
  {{#each page.section as |section|}}
    <PageSection @section={{section}} />
  {{/each}}
  <ArticleArchive @articles={{articles}} />
  {{liquid-outlet enableGrowth=false}}
</div>
